apiVersion: v1
kind: Service
metadata:
  name: {{ include "qamanager.fullname" . }}
  labels:
    {{- include "qamanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qamanager.selectorLabels" . | nindent 4 }}
